// ===========================================
// Constants
// ===========================================
// Shared constants used across the desktop application.

/**
 * Production API URL (backend)
 * This is used when no custom apiBaseUrl is configured
 */
export const PRODUCTION_API_URL = 'https://aws.collablearn.in';

/**
 * Production Web URL (frontend)
 * Used for settings links, user-facing URLs, etc.
 */
export const PRODUCTION_WEB_URL = 'https://collablearn.in';
